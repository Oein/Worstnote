name: goodnotes_parser
description: A pure-Dart parser for GoodNotes 5/6 (.goodnotes) document packages — extracts pages, strokes, text boxes, attachments, and OCR data without depending on Flutter.
version: 0.1.0
homepage: https://github.com/your-org/goodnotes_parser

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  archive: ^3.6.0

dev_dependencies:
  test: ^1.24.0
  lints: ^3.0.0
